# 绑定 host

## macOS

```bash
sudo nano /etc/hosts
```

末尾添加：`127.0.0.1 homeassistant.local`，保存生效。

## Windows

1. 管理员打开记事本，打开 `C:\Windows\System32\drivers\etc\hosts`
2. 末尾添加：`127.0.0.1 homeassistant.local`，保存生效。
